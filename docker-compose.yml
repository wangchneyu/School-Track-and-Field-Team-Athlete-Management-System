version: "3.9"

services:
  app:
    build: .
    container_name: athletics-app
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    command: uvicorn main:app --host 0.0.0.0 --port 8000
